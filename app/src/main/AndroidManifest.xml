<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sep.assignment1">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.UserSignupActivity" />
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.DeliveryMainActivity"
            android:label="@string/title_activity_delivery_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.RestaurantMainActivity"
            android:label="@string/title_activity_restaurant_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.MainSignupActivity"
            android:label="@string/title_activity_signup_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.AddRestaurantActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.AddMenuActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.AddFoodActivity"
            android:label="@string/title_activity_add_food"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.UserMainActivity"
            android:label="@string/title_activity_user_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.BalanceTopupActivity"
            android:label="@string/title_activity_balance_topup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.BalanceWithdrawn"
            android:label="@string/title_activity_balance_withdraw"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.CartActivity"
            android:label="@string/title_activity_cart"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.AccountActivity"
            android:label="@string/title_activity_account"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.DriverMain" />
        <activity
            android:name=".view.BalanceActivity"
            android:label="@string/title_activity_balance"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.RestaurantSignupActivity" />
        <activity
            android:name=".view.MenuMainActivity"
            android:label="@string/title_activity_menu_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.DriverSignupActivity"
            android:label="@string/driver_signup_activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.OrderActivity"
            android:label="@string/orderactivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.OrderListActivity"
            android:label="@string/title_activity_order_list"
            android:theme="@style/AppTheme.NoActionBar" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".view.PickupOrder"
            android:label="@string/title_activity_pickup_order"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.DriverProfile"
            android:label="@string/title_activity_driver_profile"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>